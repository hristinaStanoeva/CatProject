{
	"name": "cat-project",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "rimraf dist/ && tsc && node dist/",
		"watch": "concurrently -k -p \"[{name}]\" -n \"TypeScript,Node\" -c \"cyan.bold,green.bold\" \"npm run watch-ts\" \"npm run watch-node\"",
		"watch-ts": "tsc -w",
		"watch-node": "nodemon dist/",
		"build": "rimraf dist/ && tsc"
	},
	"author": "Pi",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^10.12.7",
		"concurrently": "^4.0.1",
		"rimraf": "^2.6.2"
	}
}
