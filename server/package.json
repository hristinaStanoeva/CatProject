{
	"name": "cat-project",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "rimraf dist/ && tsc && node dist/",
		"watch": "concurrently -k -p \"[{name}]\" -n \"TypeScript,Node\" -c \"blue.bold,yellow.bold\" \"npm run watch-ts\" \"npm run watch-node\"",
		"watch-ts": "tsc -w",
		"watch-node": "nodemon dist/",
		"build": "rimraf dist/ && tsc"
	},
	"author": "Pi",
	"license": "ISC",
	"devDependencies": {
		"@types/express": "^4.16.0",
		"@types/node": "^10.12.7",
		"concurrently": "^4.0.1",
		"rimraf": "^2.6.2"
	},
	"dependencies": {
		"express": "^4.16.4"
	}
}
